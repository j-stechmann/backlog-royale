services:
  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"
    networks:
      - backlog-network

  frontend:
    build:
      context: ./frontend
    ports:
      - "8081:8080"
    depends_on:
      - backend
    networks:
      - backlog-network

networks:
  backlog-network:
    driver: bridge
